{
	"cpu": {
		"interval": 1,
		"format": " {usage}%",
		"min-length": 6,
		"max-length": 6,
		"states": {
			"warning": 75,
			"critical": 90
		},
		"on-click-right": "hyprctl clients | grep -q 'title: cpu' && hyprctl dispatch focuswindow 'title:^(cpu)$' || kitty -T cpu -e btop -p 1"
	}
}
